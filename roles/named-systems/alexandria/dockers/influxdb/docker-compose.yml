services:
  influxdb2:
    image: influxdb:2
    # only needed for first run as it does automatic setup
    # what happens if I dont use these?
    # environment:
    #   DOCKER_INFLUXDB_INIT_MODE: setup
    #   DOCKER_INFLUXDB_INIT_USERNAME_FILE: /run/secrets/influxdb2-admin-username
    #   DOCKER_INFLUXDB_INIT_PASSWORD_FILE: /run/secrets/influxdb2-admin-password
    #   DOCKER_INFLUXDB_INIT_ADMIN_TOKEN_FILE: /run/secrets/influxdb2-admin-token
    #   DOCKER_INFLUXDB_INIT_ORG: homelab
    #   DOCKER_INFLUXDB_INIT_BUCKET: proxmox
    ports:
      - 8086:8086
      # - 9999:9999 # influxd server? http-bind-address but seems to not do anything?
    volumes:
      - ./influxdbdata:/var/lib/influxdb2:rw
      # only needed for setup
      # - ./username:/run/secrets/influxdb2-admin-username
      # - ./password:/run/secrets/influxdb2-admin-password
      # - ./token:/run/secrets/influxdb2-admin-token
